[project]
name = "duckdb-demo"
version = "0.1.0"
description = "Examples for testing DuckDB with database, CSV, JSON, and Excel files"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.2",
]
